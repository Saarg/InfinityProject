using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/*public class EnnemiGUI : MonoBehaviour
{

    public Slider healthBar;
    public Color healthcolor = new Color(1f, 0f, 0f, 0f);

    // Use this for initialization
    void OnGUI()
    {
        GUI.DrawTexture(new Rect((Screen.width / 2) - 100, 20, 5, 10), HpBarTexture);
    }

    void Start()
    {
        healthBar.maxValue = _player.maxLife;
    }

    // Update is called once per frame
    void Update()
    {
        if (_player == null)
        {
            Start();
        }

        healthBar.value = _enemiy.life;
    }
}*/
